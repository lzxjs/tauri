# Tauri应用自建服务器更新部署指南

## 一、服务器端配置

### 1. 创建更新目录结构

在你的服务器上创建以下目录结构：

```
/var/www/updates/          # 或其他Web服务器目录
├── latest.json            # 版本信息文件
└── packages/              # 安装包目录
    ├── app-0.2.0-x64.msi
    └── app-0.2.0-x64-setup.exe
```

### 2. 配置Web服务器

#### Nginx配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /updates/ {
        alias /var/www/updates/;

        # 启用CORS（如果需要）
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type";

        # 缓存控制
        add_header Cache-Control "no-cache, must-revalidate";

        # 自动索引（可选）
        autoindex on;
    }
}
```

#### Apache配置示例

```apache
<Directory "/var/www/updates">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted

    # 启用CORS
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "GET, OPTIONS"

    # 缓存控制
    Header set Cache-Control "no-cache, must-revalidate"
</Directory>
```

### 3. latest.json文件格式

创建`/var/www/updates/latest.json`文件：

```json
{
  "version": "0.2.0",
  "notes": "## 更新内容\n\n- 修复了若干bug\n- 新增了XXX功能\n- 优化了性能",
  "pub_date": "2025-12-11T10:00:00Z",
  "platforms": {
    "windows-x86_64": {
      "url": "https://your-domain.com/updates/packages/app-0.2.0-x64.msi",
      "signature": ""
    }
  }
}
```

**字段说明**：
- `version`: 新版本号（必须大于当前版本）
- `notes`: 更新说明（支持Markdown格式）
- `pub_date`: 发布日期（ISO 8601格式）
- `platforms`: 各平台的下载信息
  - `windows-x86_64`: Windows 64位
  - `darwin-x86_64`: macOS Intel
  - `darwin-aarch64`: macOS Apple Silicon
  - `linux-x86_64`: Linux 64位
- `url`: 安装包下载地址（必须是完整URL）
- `signature`: 签名（开发阶段可留空，生产环境必须配置）

## 二、应用端配置

### 1. 修改更新服务器地址

编辑`src-tauri/tauri.conf.json`：

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://your-domain.com/updates/latest.json"
      ],
      "dialog": true,
      "pubkey": ""
    }
  }
}
```

将`https://your-domain.com`替换为你的实际域名。

### 2. 启用更新检查UI

编辑`src/App.vue`，取消注释"检查更新"按钮：

找到以下被注释的代码块并取消注释：

```vue
<!-- <div class="autostart-card" @click="checkForUpdates(false)" style="cursor: pointer;">
  <div class="autostart-info">
    <span class="autostart-title">检查更新</span>
    <span class="autostart-desc">v{{ version }}</span>
  </div>
  <span style="font-size: 18px;">🔄</span>
</div> -->
```

取消注释后：

```vue
<div class="autostart-card" @click="checkForUpdates(false)" style="cursor: pointer;">
  <div class="autostart-info">
    <span class="autostart-title">检查更新</span>
    <span class="autostart-desc">v{{ version }}</span>
  </div>
  <span style="font-size: 18px;">🔄</span>
</div>
```

同时取消注释启动时的自动检查：

```javascript
// 启动时检查更新（静默模式）
checkForUpdates(true);
```

## 三、发布新版本流程

### 1. 更新版本号

编辑`src-tauri/tauri.conf.json`：

```json
{
  "version": "0.2.0"
}
```

### 2. 构建应用

```bash
npm run tauri:build
```

构建产物位于：
- Windows: `src-tauri/target/release/bundle/msi/`
- macOS: `src-tauri/target/release/bundle/dmg/`
- Linux: `src-tauri/target/release/bundle/appimage/`

### 3. 上传到服务器

```bash
# 上传安装包
scp src-tauri/target/release/bundle/msi/app_0.2.0_x64_en-US.msi \
    user@your-server:/var/www/updates/packages/app-0.2.0-x64.msi

# 更新latest.json
scp latest.json user@your-server:/var/www/updates/
```

### 4. 验证部署

访问以下URL验证：
- `https://your-domain.com/updates/latest.json` - 应返回JSON
- `https://your-domain.com/updates/packages/app-0.2.0-x64.msi` - 应开始下载

## 四、代码签名（生产环境推荐）

### 1. 生成密钥对

```bash
# 安装Tauri CLI
npm install -g @tauri-apps/cli

# 生成密钥对
tauri signer generate -w ~/.tauri/myapp.key
```

这会生成：
- 私钥：`~/.tauri/myapp.key`（保密！）
- 公钥：输出到终端

### 2. 配置公钥

将公钥添加到`tauri.conf.json`：

```json
{
  "plugins": {
    "updater": {
      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEFCQ0RFRkdISUpLTE1OT1BRUlNUVVZXWFlaYWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXowMTIzNDU2Nzg5"
    }
  }
}
```

### 3. 构建时签名

```bash
# 设置私钥路径
export TAURI_PRIVATE_KEY=$(cat ~/.tauri/myapp.key)
export TAURI_KEY_PASSWORD=""  # 如果设置了密码

# 构建
npm run tauri:build
```

构建后会生成`.sig`签名文件，需要一起上传到服务器。

### 4. 更新latest.json

```json
{
  "platforms": {
    "windows-x86_64": {
      "url": "https://your-domain.com/updates/packages/app-0.2.0-x64.msi",
      "signature": "dW50cnVzdGVkIGNvbW1lbnQ6IHNpZ25hdHVyZSBmcm9tIHRhdXJpIHNlY3JldCBrZXkKUldUTE1QQUJDREVGRw=="
    }
  }
}
```

签名内容从`.sig`文件中读取。

## 五、测试更新功能

### 1. 本地测试

1. 确保当前应用版本为`0.1.0`
2. 服务器上部署`0.2.0`版本
3. 启动应用，应自动检测到更新
4. 或点击"检查更新"按钮手动检查

### 2. 测试流程

- ✅ 检查更新API调用成功
- ✅ 弹出更新确认对话框
- ✅ 显示版本号和更新说明
- ✅ 下载进度正常
- ✅ 安装成功
- ✅ 应用自动重启

### 3. 常见问题

**问题1：检查更新失败**
- 检查服务器URL是否正确
- 检查CORS配置
- 检查latest.json格式是否正确

**问题2：下载失败**
- 检查安装包URL是否可访问
- 检查文件权限

**问题3：签名验证失败**
- 检查公钥是否正确配置
- 检查签名文件是否存在
- 开发阶段可暂时留空signature字段

## 六、自动化部署脚本

### deploy.sh示例

```bash
#!/bin/bash

VERSION="0.2.0"
SERVER="user@your-server"
REMOTE_PATH="/var/www/updates"

echo "开始部署版本 $VERSION..."

# 1. 构建应用
echo "构建应用..."
npm run tauri:build

# 2. 上传安装包
echo "上传安装包..."
scp "src-tauri/target/release/bundle/msi/app_${VERSION}_x64_en-US.msi" \
    "$SERVER:$REMOTE_PATH/packages/app-$VERSION-x64.msi"

# 3. 更新latest.json
echo "更新版本信息..."
cat > latest.json <<EOF
{
  "version": "$VERSION",
  "notes": "更新内容请查看发布说明",
  "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "platforms": {
    "windows-x86_64": {
      "url": "https://your-domain.com/updates/packages/app-$VERSION-x64.msi",
      "signature": ""
    }
  }
}
EOF

scp latest.json "$SERVER:$REMOTE_PATH/"

echo "部署完成！"
echo "访问 https://your-domain.com/updates/latest.json 验证"
```

使用方法：

```bash
chmod +x deploy.sh
./deploy.sh
```

## 七、监控和日志

### 服务器端日志

监控更新请求：

```bash
# Nginx日志
tail -f /var/log/nginx/access.log | grep "/updates/"

# Apache日志
tail -f /var/log/apache2/access.log | grep "/updates/"
```

### 应用端日志

在应用中添加更新日志（已在useUpdater.js中实现）：

```javascript
console.log('[Updater] 检查更新...');
console.log('[Updater] 发现新版本:', update.version);
console.log('[Updater] 下载进度:', progress);
```

## 八、安全建议

1. **使用HTTPS**：生产环境必须使用HTTPS
2. **配置签名**：防止中间人攻击
3. **访问控制**：限制更新服务器访问频率
4. **版本验证**：确保版本号格式正确
5. **备份旧版本**：保留历史版本以便回滚

---

**完成配置后，记得取消App.vue中的注释以启用更新检查功能！**
